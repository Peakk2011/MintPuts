import {
    pipe,
    compose,
    createState,
    injectCSS,
    injectHTML,
    injectTitle,
    get,
    include,
    processIncludes,
    AdjustHook,
    MintUtils,
    PerformanceMonitor,
    ReloadPerformanceTracker,
    clearInjectionCache,
    getInjectionStats
} from './MintUtils.js';

export const Mint = {
    pipe,
    compose,
    createState,
    injectCSS,
    injectHTML,
    injectTitle,
    get,
    include,
    processIncludes,
    AdjustHook,
    MintUtils,
    PerformanceMonitor,
    ReloadPerformanceTracker,
    clearInjectionCache,
    getInjectionStats
};